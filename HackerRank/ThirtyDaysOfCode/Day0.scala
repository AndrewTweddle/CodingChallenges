import scala.io._

object Solution {
    def main(args: Array[String]): Unit = {
        val inputString = StdIn.readLine()
        println("Hello, World.")
        println(inputString)
    }
}